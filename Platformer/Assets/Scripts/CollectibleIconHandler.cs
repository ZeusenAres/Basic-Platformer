using System;
using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class CollectibleIconHandler : MonoBehaviour
{

    /*[SerializeField] TextMeshProUGUI weightDisplay;

    void Awake()
    {

        float weightDisplay = short.Parse(this.weightDisplay.text);
        
        if (weightDisplay < 24.5)
        {
            
            this.weightDisplay.color = Color.green;
        }

        if (weightDisplay > 24.5)
        {

            this.weightDisplay.color = Color.red;
        }
    }*/
}
